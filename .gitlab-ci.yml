stages:
  - test
  - build

default:
  image: node:20
  cache:
    paths:
      - node_modules/

before_script:
  - npm ci

typecheck:
  stage: test
  script:
    - npm run typecheck

build:
  stage: build
  script:
    - npm run build
